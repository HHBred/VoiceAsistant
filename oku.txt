kodda sıkıntı olursa 
pip install --upgrade wheel   (If the code gives the wheel line) wheel hatası verirse
pip install playsound==1.2.2 (If playsound gives an error) playsound hata verirse
bu ikisini deneyin